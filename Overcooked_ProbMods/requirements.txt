# Overcooked ProbMods - Dependencies
# Probabilistic Programming for Overcooked AI
#
# Installation:
#   1. Create conda environment: conda create -n probmods python=3.10 -y
#   2. Activate: conda activate probmods
#   3. Install dependencies: pip install -r requirements.txt
#   4. Install parent package: cd .. && pip install -e . && cd Overcooked_ProbMods
#   5. Install this package: pip install -e .

# Core deep learning
torch>=2.0.0
numpy>=1.21.0,<2  # Must be <2 for PyTorch compatibility

# Probabilistic programming
pyro-ppl>=1.8.0
numpyro>=0.13.0  # Optional: faster inference backend (requires jax)

# Data and utilities
scipy>=1.7.0
pandas>=1.3.0
pyyaml>=6.0

# Visualization
matplotlib>=3.5.0
seaborn>=0.11.0

# Progress tracking
tqdm>=4.62.0

# Environment dependencies (for RL models)
gymnasium>=0.29.0
pygame>=2.0.0
opencv-python>=4.5.0,<4.10  # Must be <4.10 for numpy<2 compatibility
dill>=0.3.0
